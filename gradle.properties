# AndroidX package structure — required for modern Android libraries
android.useAndroidX=true

# Kotlin code style
kotlin.code.style=official

# Improve build performance on capable machines (optional; safe defaults)
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
